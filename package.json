{
  "name": "@mcp/dns",
  "version": "1.0.0",
  "description": "Infrastructure as Code for MCP Cloudflare DNS management",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "license": "MIT",
  "dependencies": {
    "@pulumi/cloudflare": "^6.12.0",
    "@pulumi/pulumi": "^3.214.1"
  },
  "devDependencies": {
    "@types/node": "^22.18.6",
    "prettier": "^3.7.2",
    "typescript": "^5.9.2"
  }
}
